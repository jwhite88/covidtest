import { DispayCCLogoDirective } from './dispay-cclogo.directive';

describe('DispayCCLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new DispayCCLogoDirective();
    expect(directive).toBeTruthy();
  });
});
